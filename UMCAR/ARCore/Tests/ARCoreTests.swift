import XCTest
@testable import ARCore

/// 타깃 배선 확인용. 실제 로직 테스트는 CardSelectionTests, TechCardTests에 있다.
final class ARCoreTests: XCTestCase {
    func test_targetIsWired() {
        XCTAssertTrue(true)
    }
}
